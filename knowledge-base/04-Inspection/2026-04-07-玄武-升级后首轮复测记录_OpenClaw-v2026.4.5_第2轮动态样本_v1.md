# 玄武｜升级后首轮复测记录｜OpenClaw v2026.4.5｜第 2 轮动态样本 v1

- 日期：2026-04-07
- 归属：玄武
- 类型：升级后动态样本复测记录
- 当前版本：`2026.4.5`

---

## 1. 本轮目的

本轮继续补升级后的动态样本，优先验证：
1. Curie 旧主会话链是否已从升级前硬失败转为可正常续跑；
2. 新 session 是否继续正常收口；
3. 是否出现“外层 timeout，但会话内实际正常收口”的并行样本。

---

## 2. A2｜旧主会话动态样本

### 会话
- Session Key: `agent:curie:main`

### 触发内容
- `【升级后复测-A2】只做最小动态样本复测...`

### 结果
- `1. 已收到`
- `2. 当前能正常续跑`
- `3. 这条回复未调用工具`

### 当前判断
> **升级后 Curie 旧主会话链已出现明确正向收口样本。**

这与升级前 A 组“user 已入链但 assistant 连续 Request timed out.” 形成正面对照，属于明显收敛信号。

---

## 3. B2｜新 session 并行样本

### 会话
- Session Key: `agent:curie:discord:channel:1481719718078451786`

### 外层结果
- `sessions_send` 返回：`timeout`

### 会话内实际结果
回看历史可见：
- `1）已收到`
- `2）能正常续跑`
- `3）未调用工具`

### 当前判断
> **B2 当前成立为：外层发送超时，但会话内实际已正常收口。**

因此不能把 B2 写成“新 session 失败”。

---

## 4. D1｜Discord 可达性动态样本

### 会话
- Session Key: `agent:curie:discord:channel:1488590483340001400`

### 结果
- `1）已收到`
- `2）当前能正常在 Discord 链路收口`
- `3）这条回复未调用工具`

### 当前判断
> **升级后 Discord 链路已拿到正向可达性样本，当前未见新增“在线假象但实际不收口”的硬故障。**

---

## 5. C1｜context 边界观察样本

### 会话
- Session Key: `agent:curie:main`

### 外层结果
- `sessions_send` 返回：`timeout`

### 会话内观察
回看历史时，可见该会话在本轮前后仍混入了与当前升级复测无直接关系的内容，例如：
- `spawn-template-chain-trial-note`
- `OpenClaw Runtime Pipeline v3 BOSS阅读版`
- 其他旧主会话高上下文内容

### 当前判断
> **这条目前还不能直接定成“上下文污染已成立”，但至少说明旧主会话上下文面仍偏脏，不适合把“能续跑”直接等同于“context 边界已干净”。**

---

## 6. 本轮最小结论

### 已成立
1. 升级后，Curie 旧主会话链已从升级前的硬失败样本，转为可正常续跑；
2. 新 session 继续可正常收口；
3. 当前仍存在 `sessions_send` 外层 timeout / delivery 表象，与会话内真实收口不完全一致的样本；
4. Discord 可达性已拿到正向样本。

### 未成立
1. 不能直接写“所有 reply/delivery 问题已闭环”；
2. 不能直接写“外层 timeout 已解决”；
3. 不能直接写“context 边界已收干净”；
4. 不能直接写“系统级完全放行”。

---

## 7. 一句话收口

> **第 2 轮动态样本继续补强了升级后的主链收敛：旧主会话能续跑、Discord 能收口、新 session 内层正常；但外层 timeout 仍在，旧主会话的 context 边界也仍偏脏。**
