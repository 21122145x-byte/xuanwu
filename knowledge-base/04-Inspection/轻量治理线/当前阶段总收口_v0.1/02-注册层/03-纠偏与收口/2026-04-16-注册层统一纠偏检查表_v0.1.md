# 注册层统一纠偏检查表 v0.1

- 日期：2026-04-16
- 起草：粉雪
- 状态：drafting
- 性质：注册层一致性检查表 / 小步纠偏记录
- 用途：对当前注册层主链做统一纠偏检查，记录检查范围、发现问题、已修正项、暂不修正项与当前一致性结论。
- 版本目录：轻量治理线 / 当前阶段总收口_v0.1
- 所属层级：注册层 / 纠偏与收口
- 当前阶段：注册层骨架一致性可接受 / 边界仍待继续清理
- 检查范围：
  - `2026-04-14-注册层整理表_v0.1.md`
  - `2026-04-15-现役能力清单_v0.2.md`
  - `2026-04-15-机制与skill映射表_v0.1.md`
  - `2026-04-16-默认调用顺序表_v0.1.md`
  - `2026-04-16-废弃入口与辅助入口标记表_v0.1.md`
  - `2026-04-16-冲突裁决优先级表_v0.1.md`
  - `2026-04-16-按任务类型读取表_v0.1.md`
  - `2026-04-16-注册层状态口径对齐卡_v0.1.md`
  - `README.md`

---

## 一、检查结论概览

本轮先审后改，只做低风险、小步纠偏。
当前主链已基本形成：
- 整理表负责展开骨架
- 现役能力清单负责机制完成度
- 映射表负责机制 ↔ skill 关系
- 默认调用顺序 / 入口标记 / 冲突裁决 / 按任务类型读取，已形成一组可互相咬合的正式表件
- 状态口径对齐卡负责跨表状态翻译

当前判断：**已达到“骨架一致性可接受”状态，但仍未达到“细节完全无歧义”状态。**

---

## 二、发现的问题

### 1. 已补件仍写“待补”
- `2026-04-16-默认调用顺序表_v0.1.md` 的下位文档仍写“待补：废弃入口 / 辅助入口标记表 / 冲突裁决优先级表 / 按任务类型读取表”。
- `2026-04-16-废弃入口与辅助入口标记表_v0.1.md` 的下位文档仍写“待补：冲突裁决优先级表 / 按任务类型读取表”。

### 2. 已补件后，旧 next steps 未更新
- `2026-04-14-注册层整理表_v0.1.md` 末尾仍写“后续继续把现役能力清单 / 默认调用顺序表 / 废弃入口标记表 / 冲突裁决优先级表 / 按任务类型读取表逐项补出来”，与当前实际状态不一致。
- `2026-04-16-默认调用顺序表_v0.1.md` 的 Known Issues / Next Steps 仍保留“尚未单独补出”的旧措辞。

### 3. 重复挂载 / 旧版本说明容易混读
- `2026-04-14-注册层整理表_v0.1.md` 的下位文档中，`默认调用顺序表_v0.1.md` 出现重复挂载。
- `README.md` 同时挂了 `现役能力清单_v0.1` 与 `v0.2`，但原说明未显式提示 v0.1 已被 v0.2 纠偏，容易让人误读为并列现役口径。

### 4. 仍存在但本轮未直接改的轻微不一致
- `现役能力清单_v0.2` 中部分“已有 skills 清单”更像跨环境总表，不完全等于本工作区当前可直接调用的 skills 视图。
- `README.md` 仍以“孤儿文件索引”为标题，当前已兼具主链导航作用，但其整体结构仍偏“手动补挂 + 历史索引混排”。

---

## 三、已修正项

1. 已将 `默认调用顺序表_v0.1` 中的“待补”改为实际已存在文档链接。
2. 已将 `废弃入口与辅助入口标记表_v0.1` 中的“待补”改为实际已存在文档链接。
3. 已更新 `默认调用顺序表_v0.1` 的 Known Issues / Next Steps，使其与当前补件状态一致。
4. 已移除 `注册层整理表_v0.1` 中对 `默认调用顺序表_v0.1` 的重复挂载。
5. 已更新 `注册层整理表_v0.1` 末尾 Next Steps，避免继续保留“已补却仍写待补”的残留措辞。
6. 已在 `README.md` 中给 `现役能力清单_v0.1` 加注“首版，后续已由 v0.2 做口径纠偏”，降低版本混读风险。

---

## 四、暂不修正项

### 1. `现役能力清单_v0.2` 的已有 skills 清单是否应改为“当前环境可用 skills 清单”
暂不直接改。
原因：这会牵涉“文档记录的是团队总资产，还是当前环境注册层”的范围裁定，不属于低风险文字纠偏。

### 2. README 是否应重构为“当前主链导航 README”
暂不直接改。
原因：这已超出小步纠偏，涉及目录导航结构重写；本轮只先清旧措辞，不做大重构。

### 3. 状态词是否统一补成双层表达（如 完整 + active）
暂不直接改。
原因：对齐卡已经给出跨表翻译规则，若全量回写到各文档，会形成中等规模改动。

---

## 五、当前一致性结论

### 已达成的一致性
- 主链分工已基本清楚：整理 / 清单 / 映射 / 顺序 / 标记 / 裁决 / 类型读取 / 状态对齐，各自职责没有明显打架。
- “默认先读谁”“哪些只是辅助视图”“口径冲突先信谁”“按任务类型怎么进”四层口径已能互相闭合。
- 已补件对象与整理表中的“已补”描述，现已基本对齐。

### 仍保留的弱点
- README 仍偏历史索引型，不是纯净导航页。
- 现役能力清单中的“skills 总表”与“当前环境注册层”之间，仍有潜在边界混用。
- 少数文档仍停留在 `drafting`，状态成熟度与内容成熟度未完全同步。

### 当前总判断
> **当前注册层已达到“骨架一致性可接受”状态。**
> 也就是说：主链已能稳定作为阅读与引用骨架使用；但若继续追求“调用即无歧义”，还需要做一轮更细的边界清理。

---

## 六、下一步建议

1. 做一轮“README 轻导航化”整理，但只限增补当前主链入口，不做整目录重构。
2. 单独核一遍 `现役能力清单_v0.2` 的 skills 段，明确它记录的是“团队总资产”还是“当前环境现役”。
3. 选 2-3 个最常用任务场景，做一次从 README → 注册层表件 → 候选链的实走读，看是否仍会误入旧入口。
4. 若后续继续收口，可考虑补一张“注册层正式件总览卡”，把当前主链正式件收成一页。

---

## 七、5 字段 handoff
- **Where**
  - `2026-04-16-注册层统一纠偏检查表_v0.1.md`
- **What**
  - 对注册层主链做了一轮统一纠偏检查，并记录发现问题、已修正项、暂不修正项与一致性结论。
- **How to verify**
  - 对照检查范围逐一核查：是否仍存在“已补却写待补”、重复挂载、旧 next steps 残留、README 版本混读风险。
- **Known Issues**
  - README 结构仍偏历史索引；现役能力清单中的 skills 段仍可能存在环境边界混用。
- **Next Steps**
  - 做 README 轻导航化与 skills 段边界核定，作为下一轮低风险收口。
