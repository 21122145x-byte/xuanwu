# 玄武｜升级后首轮复测记录｜OpenClaw v2026.4.5｜第 1 轮 v1

- 日期：2026-04-07
- 归属：玄武
- 类型：升级后首轮复测记录
- 对应清单：`/Users/xiaojingbo/.openclaw/workspace/knowledge-base/04-Inspection/2026-04-07-升级后首轮复测清单_Discord离线_上下文异常专项版_OpenClaw-v2026.4.5_v1.md`
- 当前版本：`2026.4.5`

---

## 1. 本轮目的

本轮先收升级后的**静态运行状态样本**与**主链可见状态样本**，优先回答：
1. gateway / node / channel 是否已经稳定恢复；
2. `openclaw-weixin` 清理后，插件错误是否仍残留为“当前错误”；
3. Discord 当前是否呈现明显离线 / 假离线；
4. 当前主链上是否已经出现新的上下文污染硬样本。

本轮暂未主动注入新的对话测试消息，先冻结“升级后第一层状态样本”。

---

## 2. 运行态样本

### 2.1 Gateway / Node / Channel
根据 `openclaw status`：
- Gateway：`reachable 29ms`
- Gateway service：`loaded · running (pid 84717)`
- Node service：`loaded · running (pid 10587)`
- Channels：
  - Discord：`ON / OK / accounts 6/6`
  - Feishu：`ON / OK / accounts 6/6`
- Update：`up to date · 2026.4.5`

### 当前判断
> **升级后主运行态已恢复，gateway / node / Discord / Feishu 当前都处于在线可达状态。**

---

## 3. 插件与启动链样本

### 3.1 doctor 结果
`openclaw doctor` 当前显示：
- Plugins: `Errors = 0`
- Discord: `ok`
- Feishu: `ok`

### 3.2 gateway.err.log 尾部观察
当前尾部仍可见 `07:59 ~ 08:01` 的历史崩溃期样本：
- `openclaw-weixin failed to load ... resolvePreferredOpenClawTmpDir is not a function`

但在当前轮次未看到新的同类实时报错；结合：
- `openclaw-weixin` 扩展目录已清理
- doctor 插件错误归零

### 当前判断
> **`openclaw-weixin` 当前不再构成“实时插件错误”，日志中保留的是历史崩溃期样本。**

---

## 4. Discord 离线 / 状态一致性样本

### 当前可见状态
- `openclaw status` 中 Discord = `OK`
- `openclaw doctor` 中 Discord = `ok (@粉雪minimax)`
- 当前未出现新的“Discord 显示离线但链路实际可用”硬样本
- 当前也未出现新的“Discord 显示在线但完全不可达”硬样本

### 当前判断
> **第 1 轮未见新增 Discord 离线 / 假离线硬样本。**

### 备注
这条目前只是**状态层初步正常**，还不等于：
- ready / reconnect / stale socket 风险已闭环
- Discord 状态一致性问题已彻底修复

---

## 5. 上下文异常 / session 边界样本

### 当前可见会话面
`sessions_list` 显示：
- `agent:curie:main` 当前为 `running`
- `agent:system-guardian:main` 当前为 `running`
- 暂未在本轮拿到新的 thread / reset / context reinject 对话硬样本

### 邻接信号
本轮日志里仍见到的并行异常：
- `judgeNewTopic failed ... 404`
- 若干 `Skipping skill path that resolves outside its configured root`
- memory 文件不存在导致的 `read ENOENT`

### 当前判断
> **第 1 轮尚未拿到新的“上下文污染 / reset 不真重置 / thread 串扰”硬样本，但 topic judge 404 仍在，说明上下文相关支线还未完全干净。**

---

## 6. 当前已成立 / 未成立

### 6.1 已成立
1. 升级后的主运行态已恢复；
2. `openclaw-weixin` 已从实时故障面退出；
3. Discord 当前状态面显示正常，未见新增离线硬样本；
4. 第 1 轮可将 `weixin` 问题从“当前阻断”降为“历史崩溃样本”。

### 6.2 未成立
1. 尚不能写 Discord 离线 / 假离线问题已闭环；
2. 尚不能写上下文异常问题已闭环；
3. 尚不能写旧主会话链与新 session 差异已收敛；
4. 尚不能写 topic judge 404 对主链无影响。

---

## 7. 当前风险

### 已收住
- `openclaw-weixin` 插件兼容断裂造成的启动阻断

### 仍在观察
- Discord `groupPolicy=open` 带来的开放面风险
- topic judge 404
- skill path outside configured root
- memory 文件缺失导致的 read ENOENT
- 旧主会话链 / reply delivery / context 边界问题是否仍存在

---

## 8. 下一步

### 下一步最优先
1. 补一轮**动态对话样本复测**：
   - Curie 旧主会话链
   - 新 session 对照
2. 补一轮**Discord 离线 / 可达性动作复测**
3. 补一轮**thread / reset / context 边界**动作复测

### 当前口径
> 第 1 轮先确认了“系统已经重新站起来”，但还没有资格写“异常已经闭环”。

---

## 9. 一句话收口

> **升级后首轮复测第 1 轮已确认：OpenClaw `2026.4.5` 当前主运行态恢复正常，`openclaw-weixin` 已退出实时故障面，Discord 状态层未见新增离线样本；但上下文异常、旧主会话链与 topic judge 支线仍需继续补动态样本。**
