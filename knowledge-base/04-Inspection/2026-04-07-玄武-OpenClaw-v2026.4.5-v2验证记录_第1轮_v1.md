# 玄武｜OpenClaw v2026.4.5 v2 验证记录｜第 1 轮 v1

- 日期：2026-04-07 00:20（Asia/Shanghai）
- 归属：玄武
- 类型：专项验证记录
- 对应计划：`/Users/xiaojingbo/.openclaw/workspace/knowledge-base/04-Inspection/2026-04-07-玄武-OpenClaw-v2026.4.5-专项验证计划_v2.md`

---

## 1. 本轮目的

先跑 v2 中最轻的两针：
- A：旧主会话 / 纯 user-after
- B：新 session / 纯 user-after

目标：
- 先判断是否已出现 `E1 / E2 / E3` 的第一层分叉
- 暂不引入 toolResult，避免第一轮噪音过大

---

## 2. A 组记录

### 基本信息
- Group: A
- Agent: Curie
- Session Key: `agent:curie:main`
- Session ID: `83fdefe4-529e-4515-84ce-83572b7f5857`
- Session Type: old
- Surface: Discord
- `/models` 历史: 有
- Model Route: `tokenx24-40 / gpt-5.4 / openai-responses`
- Trigger Type: user
- ToolResult Complexity: 无

### 触发内容
发送：
- `【v2验证-A】只做纯 user-after 最小测试。请只回复这 3 项，不要调用工具...`

### 观测结果
- user 消息已进入 Curie 主会话历史
- 后续未见正常 assistant 文本收口
- 会话内连续出现两条 assistant error：
  - `Request timed out.`
  - `Request timed out.`
- `sessions_send` 外层也返回：
  - `gateway timeout after 32000ms`

### 事件类型判定
- 当前更接近：`E1｜未续跑`

### 邻接信号
- session rotation: 当前未见直接证据
- ownership change: 当前未见直接证据
- active run mismatch: 当前未见直接证据，但外层 gateway timeout + 会话内 assistant error 并存，值得继续盯

### 结果
- Result: `timeout`
- Symptoms:
  - 无正常收口
  - 外层超时
  - 会话内 assistant error

### 初步判断
> A 组旧主会话在最轻 user-after 条件下再次出现超时类失稳，且这次不是“消息没送到”，而是**消息送达后 assistant 侧超时**。

---

## 3. B 组记录

### 基本信息
- Group: B
- Agent: Curie
- Session Key: `agent:curie:discord:channel:1481719718078451786`
- Session ID: `316e9adb-5106-4f93-8fc5-b6a10e03da63`
- Session Type: new
- Surface: Discord
- `/models` 历史: 未确认
- Model Route: `tokenx24-40 / gpt-5.4 / openai-responses`
- Trigger Type: user
- ToolResult Complexity: 无

### 触发内容
第一次发送：
- `【v2验证-B】只做纯 user-after 最小测试...`

第二次发送：
- `【v2验证-B重试】纯 user-after 最小测试...`

### 观测结果
- 两次 `sessions_send` 外层都返回：`timeout`
- 当前拉取会话历史时，尚未看到这两次新测试消息进入历史
- 当前也未看到对应 assistant 收口

### 事件类型判定
- 当前暂定：`未判定`

### 邻接信号
- session rotation: 未见
- ownership change: 未见
- active run mismatch: 未见直接证据

### 结果
- Result: `partial`
- Symptoms:
  - 外层发送超时
  - 当前无法确认消息是否真正入会话

### 初步判断
> B 组当前拿到的是**外层发送超时样本**，但还不能直接下成 E1/E2/E3；需要继续核是否是 delivery 未入链，还是只是历史刷新延迟。

---

## 4. 本轮最小结论

### 已成立
1. A 组已形成一条有效样本：
   - **旧主会话 / 纯 user-after / 消息已入会话 / assistant 连续超时**
2. 这条样本支持：
   - 旧主会话在最轻条件下仍存在失稳
   - 当前更偏 `E1｜未续跑`

### 尚未成立
1. B 组是否稳定优于 A 组
2. B 组是否属于：
   - 消息未入链
   - 已入链未续跑
   - 已续跑未投递
3. active run / ownership / delivery 哪条更贴近当前主故障点

---

## 5. 下一步

### 下一步最优先
1. 继续补 B 组确认样本
   - 核消息是否真正进入会话
   - 若进入，再判 E1 / E2 / E3
2. 在 A 组继续补 A2
   - 进入 `toolResult-after` 轻量测试
3. 继续记录邻接信号：
   - session rotation
   - ownership change
   - active run mismatch

### 当前口径
> 第 1 轮已经坐实：A 组旧主会话在 user-after 最轻条件下仍可失稳；B 组当前只拿到发送层超时，尚不足以判断新 session 是否更稳。

---

## 6. 一句话收口

> 第 1 轮 v2 验证已拿到第一条硬样本：**Curie 旧主会话在 user-after 条件下，消息已入会话但 assistant 侧连续 `Request timed out.`**；这更接近 `E1｜未续跑`，而 B 组仍需继续补证，当前不能草率写成“新 session 更稳”。